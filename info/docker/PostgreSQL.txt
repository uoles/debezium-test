https://habr.com/ru/articles/578744/

docker pull postgres
------------
docker run --name test-postgres-source -e POSTGRES_PASSWORD=mysecretpassword -e POSTGRES_USER=dbz -e POSTGRES_DB=testdbz -p 5432:5432 -d postgres

docker run --name test-postgres-target -e POSTGRES_PASSWORD=mysecretpassword -e POSTGRES_USER=dbz -e POSTGRES_DB=testdbz -p 5433:5432 -d postgres
------------
docker run --name test-postgres-source -e POSTGRES_PASSWORD=mysecretpassword -e POSTGRES_USER=dbz -e POSTGRES_DB=testdbz -p 5432:5432 -v "e:\Sourses\Java\_docker\postgresql-source\data":"/var/lib/postgresql/data" -d postgres

docker run --name test-postgres-target -e POSTGRES_PASSWORD=mysecretpassword -e POSTGRES_USER=dbz -e POSTGRES_DB=testdbz -p 5433:5432 -v "e:\Sourses\Java\_docker\postgresql-target\data":"/var/lib/postgresql/data" -d postgres
------------

CREATE CHEMA dbz;

CREATE TABLE dbz.customer
(
    id integer NOT NULL,
    fullname character varying(255),
    email character varying(255),
    CONSTRAINT customer_pkey PRIMARY KEY (id)
);

SELECT * FROM pg_settings WHERE name ='wal_level';

ALTER SYSTEM SET wal_level = logical;

INSERT INTO dbz.customer (id, fullname, email) VALUES (1, 'John Doe', 'jd@example.com')

UPDATE dbz.customer t SET t.email = 'john.doe@example.com' WHERE t.id = 1

DELETE FROM dbz.customer WHERE id = 1

