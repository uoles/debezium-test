CREATE SCHEMA dbz;

CREATE TABLE dbz.customer
(
    id integer NOT NULL,
    fullname character varying(255),
    email character varying(255),
    CONSTRAINT customer_pkey PRIMARY KEY (id)
);

-- для получения полной информации при обновлении записи (состояние до и после)
ALTER TABLE dbz.customer REPLICA IDENTITY FULL;

SELECT * FROM pg_settings WHERE name = 'wal_level';

CREATE PUBLICATION dbz_publication FOR TABLES IN SCHEMA dbz;

-- для работы cdc нужно установить уровень журнала logical и перезагрузить базу
ALTER SYSTEM SET wal_level = logical;

INSERT INTO dbz.customer (id, fullname, email) VALUES (3, 'John Doe', 'jd@example.com');

UPDATE dbz.customer SET email = 'john.doe@example.com2' WHERE id = 1;

DELETE FROM dbz.customer WHERE id = 1;

select slot_name, active
from pg_replication_slots;

select *
from pg_catalog.pg_publication;

SELECT
   slot_name,
   active,
   pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) as slot_lag,
   pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) as confirmed_lag
 FROM pg_replication_slots
 WHERE slot_name = :slotName


select *
from dbz.debezium_offset_storage;

drop table dbz.debezium_offset_storage;



select *
from dbz.customer;


